[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --strict-markers
    -v
    --tb=short

markers =
    unit: Unit tests (fast, no I/O)
    integration: Integration tests (may require network or services)
    slow: Tests that take more than 1 second
